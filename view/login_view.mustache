{{> header}}
<!-- Quizzium Lobby -->
<div class="container">
    <img src="../public/assets/characters.png" class="characters">

    <form action="/login/validate" method="post" enctype="multipart/form-data" class="login-area">

        {{#contrasenia}}
            <div class="field">
                <p>contraseña o mail incorrectos</p>
            </div>
        {{/contrasenia}}

        {{#validacion}}
            <div class="field">
                <p>cuenta validada con exito</p>
            </div>
        {{/validacion}}


        <div class="field">
            <label>Ingrese su mail</label>
            <input type="email" name="login[mail]"/>
        </div>

        <div class="field">
            <label>Ingrese su contraseña</label>
            <input type="password" name="login[password]"/>
        </div>

        <button type="submit" class="button button-small">
            Ingresar
        </button>
    </form>

    <a href="/register/list" class="button button-small">
        Registrarse
    </a>

</div>

<!-- End Page Content -->
</div>
{{> footer}}